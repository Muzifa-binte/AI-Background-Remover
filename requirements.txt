# =========================================================
# AI Background Removal System - Python Requirements
# Covers: Team 1 (AI Model) + Team 2 (Backend/FastAPI)
# =========================================================

# ---------- Core Deep Learning ----------
torch==2.3.1
torchvision==0.18.1
# Use the matching CUDA build if deploying on GPU, e.g.:
# --extra-index-url https://download.pytorch.org/whl/cu121

# ---------- AI Model Team: Segmentation Models ----------
# U^2-Net, BiRefNet, RMBG-2.0 dependencies
transformers==4.44.2          # for RMBG-2.0 (Hugging Face) and BiRefNet
huggingface-hub==0.24.6
timm==1.0.9                   # backbone architectures used by BiRefNet
safetensors==0.4.4
einops==0.8.0                 # tensor ops used in BiRefNet

# ---------- Image Processing ----------
opencv-python==4.10.0.84
Pillow==10.4.0
numpy==1.26.4
scikit-image==0.24.0          # edge refinement / mask post-processing
rembg==2.0.59                 # optional: prebuilt U^2-Net pipeline wrapper

# ---------- Optimization / Inference ----------
onnx==1.16.2                  # optional: export for faster CPU inference
onnxruntime==1.19.2           # CPU inference acceleration
onnxruntime-gpu==1.19.2       # GPU inference acceleration (use instead of CPU build)

# ---------- Backend Team: FastAPI ----------
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9       # required for file/image upload handling
pydantic==2.9.2
pydantic-settings==2.5.2

# ---------- Backend Team: MongoDB ----------
pymongo==4.8.0
motor==3.5.1                  # async MongoDB driver for FastAPI
beanie==1.26.0                # optional: ODM built on Motor + Pydantic

# ---------- Backend Team: File / Auth / Utils ----------
python-jose[cryptography]==3.3.0   # JWT (if auth is added)
passlib[bcrypt]==1.7.4             # password hashing (if auth is added)
aiofiles==24.1.0                   # async file I/O for uploads
python-dotenv==1.0.1                # environment variable management
loguru==0.7.2                       # structured logging

# ---------- Testing ----------
pytest==8.3.3
httpx==0.27.2                       # for testing FastAPI endpoints
